# ============================================
# HOSTEL MANAGEMENT SYSTEM - ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and fill in your actual values
# For production deployment, use these same variables in Render Dashboard

# ============================================
# SERVER CONFIGURATION
# ============================================
NODE_ENV=development
PORT=3000

# ============================================
# DATABASE (MongoDB)
# ============================================
# Local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/hostel_management

# MongoDB Atlas (Recommended):
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/hostel_management

# Get MongoDB Atlas connection string:
# 1. Go to https://cloud.mongodb.com
# 2. Create free cluster (M0)
# 3. Database Access → Add user
# 4. Network Access → Add IP (0.0.0.0/0 for Render)
# 5. Connect → Connect your application → Copy connection string

# ============================================
# JWT AUTHENTICATION
# ============================================
JWT_SECRET=your_secure_random_jwt_secret_here_change_this
JWT_EXPIRE=7d

# Generate secure JWT_SECRET:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=priyanshuguptaiitian9696@gmail.com
EMAIL_PASS=wtawvlkxlcmbtogw

# Get Gmail App Password:
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Generate app password
# 3. Use the 16-character password here

# ============================================
# GOOGLE OAUTH (Optional)
# ============================================
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/google-callback.html


# Setup Google OAuth:
# 1. Go to https://console.cloud.google.com/
# 2. Create new project
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/google/callback (development)
#    - https://your-app.onrender.com/api/auth/google/callback (production)

# ============================================
# CORS CONFIGURATION (Production)
# ============================================
# Comma-separated list of allowed origins
# ALLOWED_ORIGINS=https://your-app.onrender.com,https://your-custom-domain.com

# ============================================
# NOTES FOR DEPLOYMENT
# ============================================
# 
# LOCAL DEVELOPMENT:
# - Copy this file to .env
# - Fill in your values
# - Use PORT=3000 or any available port
# - Use local MongoDB or Atlas
#
# RENDER DEPLOYMENT:
# - Don't commit .env file (it's in .gitignore)
# - Add these variables in Render Dashboard → Environment
# - Change NODE_ENV to 'production'
# - Use MongoDB Atlas (not local)
# - Update GOOGLE_REDIRECT_URI to your Render URL
# - PORT is automatically set by Render (don't set it)
#
# REQUIRED FOR PRODUCTION:
# - NODE_ENV=production
# - MONGODB_URI (MongoDB Atlas)
# - JWT_SECRET (strong random string)
# - JWT_EXPIRE=30d
#
# OPTIONAL FOR PRODUCTION:
# - EMAIL_* (if using email features)
# - GOOGLE_* (if using Google OAuth)
# - ALLOWED_ORIGINS (for CORS)
#
